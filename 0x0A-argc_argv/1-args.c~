#include <stdio.h>

/**
 *main - entry point
 *Description: prints the number of arguments
 *@argc: argunment counter
 *@argv: an array
 *Return: 0 (success)
*/

int main(int argc, char *argv[])
{
(void)argv[];
printf("%d\n", argc);
return (0);
}
