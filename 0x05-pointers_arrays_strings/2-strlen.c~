#include "main.h"

/**
 * _strlen - returns the length of string
 * @s: string parameter input
 * Return: length of  string
*/

int _strlen(char *a)
{
int counter;
for (counter = 0; *s != '\0'; s++)
++counter
return (counter);
}
